import { ConsultaidPipe } from './consultaid.pipe';

describe('ConsultaidPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultaidPipe();
    expect(pipe).toBeTruthy();
  });
});
